#include <string>

class Pos {
    public: 
    float x;
    float y; 
    float z;
    
    Pos(float x, float y, float z) : x(x), y(y), z(z){}

    std::string posS(){
      // bah strings
    }
};
